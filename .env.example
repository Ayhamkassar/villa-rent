# Villa Rent API - Environment Variables
# Copy this file to .env and fill in your values

# =====================
# Required Variables
# =====================

# MongoDB connection string
MONGO_URI=mongodb+srv://kassara653:LseQQYXxJmzpz8Is@cluster0.lrh6drr.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
# Or MongoDB Atlas: mongodb+srv://user:password@cluster.mongodb.net/villa-rent

# JWT secret for authentication tokens (generate a secure random string)
JWT_SECRET=A_ReAl_S2Cr1t_Th4t_7oU_w!lL_NeVeR

# =====================
# SMTP Email Configuration
# =====================

# Gmail SMTP settings (recommended for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false

# Your Gmail address
SMTP_USER=kassaraeham067@gmail.com

# App password (NOT your Gmail password!)
# To generate an app password:
# 1. Go to Google Account > Security
# 2. Enable 2-Step Verification if not already enabled
# 3. Go to App passwords
# 4. Create a new app password for "Mail"
# 5. Copy the generated 16-character password here
SMTP_PASS=gmpn kiwg itmj sfel

# Display name for sent emails
EMAIL_FROM_NAME=Villa Rent

# =====================
# Optional Variables
# =====================

# Server port (default: 3000)
PORT=3000

# Environment (development/production)
NODE_ENV=development

# Base URL for the API
BASE_URL=http://localhost:3000

# Allowed CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:19006,http://localhost:3000
